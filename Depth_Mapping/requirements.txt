# Depth Mapping Part 1 -- core runtime dependencies
# =================================================
# Install with:
#   pip install -r requirements.txt
#
# GPU (strongly recommended):
#   Install PyTorch with CUDA *before* running this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
#   (adjust cu124 to match your CUDA version)
#
# Then install the rest:
#   pip install -r requirements.txt

# Core pipeline
torch>=2.0
pillow>=9.0
numpy>=1.24
matplotlib>=3.7
rasterio>=1.3

# Testing
pytest>=7.4

# Optional: GAMUS height-evaluation metrics
# Uncomment if using depth_mapping.gamus.evaluate
# scipy>=1.10

# Optional: HuggingFace streaming for GAMUS dataset access
# Uncomment if using depth_mapping.gamus.dataset.stream_gamus()
# datasets>=2.0
